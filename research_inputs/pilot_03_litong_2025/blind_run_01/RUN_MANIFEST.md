# RUN_MANIFEST

## 1. 运行身份与配置

- **实验身份**：Pilot 03 正式独立盲 Builder。
- **运行方式**：单一 Builder、标准模式；未启用子 Agent 或多 Agent。
- **请求模型**：`gpt-5.6-sol`。
- **实际可观察模型标识**：`gpt-5.6-sol`。
- **推理档位**：`high`。
- **协议版本**：`gdu-builder-protocol-v2`。
- **完整有效主运行数**：1；接受第一次完整有效主运行，未生成候选供选择。

## 2. 时间与停止状态

- **任务开始时间**：`2026-08-19T13:52:37+0800`。
- **主干稳定及停止门完成时间**：`2026-08-19T14:01:40+0800`。
- **任务结束时间**：`2026-08-19T14:05:48+0800`。
- **时区**：Asia/Shanghai（UTC+08:00）。
- **最终停止状态**：`formal_complete`。
- **联合停止门**：覆盖度通过；证据度通过；稳定度通过。
- **强制子检查**：跨表达载体冲突、跨章节功能关系、限制与负面推断边界均为“已检查并形成结果”。
- **有界定向修正**：1/2；修正后停止门通过，未启动第二次。

## 3. 输入门与哈希

运行开始时，工作区顶层恰有四个白名单普通文件且无其他条目：`paper.pdf`、`paper.txt`、`BUILDER_PROTOCOL_V2.md`、`INPUT_MANIFEST.sha256`。`INPUT_MANIFEST.sha256` 的三项校验均通过。

| 文件 | 字节数 | SHA-256 |
|---|---:|---|
| `paper.pdf` | 1,554,048 | `fbb9875c7eca1f921ca0635cabbe53727b7ff57658750fa0eeefd92402730c59` |
| `paper.txt` | 455,096 | `d3258943647ba57408471fd43ece8d52415e75ec4f39df16c63861d4af450c9a` |
| `BUILDER_PROTOCOL_V2.md` | 15,267 | `072d827ea6b8cba103af6ffbd3767a4094fc2f4825e51dedc8d921fa513be041` |
| `INPUT_MANIFEST.sha256` | 241 | `f782aa4381ec515d53a81be2e6566feec5da4349d74213622ff9ff018f38175c` |

## 4. 来源与隔离执行

- `paper.pdf` 是唯一权威内容来源；`paper.txt` 只用于关键词搜索、候选位置发现和导航。
- 已完整逐页读取 237 个 PDF 物理页的文本层，并对高影响表格和跨载体疑点回到 PDF 版面核验。
- 未联网，未搜索或读取 Gold、评分规则、讨论日志、既有中文解读或外部论文材料。
- 未使用子 Agent；没有从其他任务继承或共享答案文件。
- 本地应用提供的 PDF 解析/渲染依赖只执行源文件读取与版面核验，不作为内容来源；核验用临时页图已在冻结前删除。
- 隔离不能证明模型预训练中从未出现该报告；该限制不作推测性消解。

## 5. 技术事件与重试

- **完整主运行技术重试次数**：0。
- **主运行重新生成次数**：0；未因结果质量、预期差异或潜在评分表现重跑。
- `2026-08-19T13:53:02+0800`：默认 `python3` 缺少 `pypdf`，首次 PDF 解析探测报 `ModuleNotFoundError`；当时未产生正式输出、输入未损坏、主运行未中断或重启。随后改用应用提供的本地只读 PDF 依赖继续，记为一次底层工具回退，不是模型/主运行重试。
- 冻结前首次编号闭包自动检查因正则转义过严而返回失败；该只读检查未修改正式文件，随后简化表达式复核通过。记为一次辅助质量检查命令纠正，不是主运行重试或结果重生成。
- 未发生服务错误、输入损坏、输出损坏或模型静默切换。

## 6. 输出与冻结规则

本次输出包包括：

- `BUILDER_OUTPUT.md`：七部分结构化理解结果；
- `BUILD_TRACE.md`：三轮构建、关键修订、强制子检查和停止门记录；
- `RUN_MANIFEST.md`：本运行清单；
- `OUTPUTS.sha256`：上述前三个文件的 SHA-256。

`OUTPUTS.sha256` 生成并校验通过后，前三个文件即冻结，不再原位修改。应用无法显示可核验的精确后端模型快照，因此仅记录界面可观察模型标识，不猜测底层版本；这是复现限制。
