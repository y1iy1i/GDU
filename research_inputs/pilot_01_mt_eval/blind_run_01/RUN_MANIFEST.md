# Pilot 01 盲 Builder 运行清单

## 运行身份

- 运行名称：Pilot 01 单一盲 Builder 主运行
- 运行状态：第一次完整有效主运行，正式完成
- 协议版本：`pilot-01-builder-protocol-v1`
- 协议 SHA-256：`3fed6a5ce3d4f7df0d99c6a49576ce9a4932d2097e0d51c93bbe9f32c33aedac`
- 请求模型：`gpt-5.6-sol`
- 可观察实际模型标识：`gpt-5.6-sol`
- 推理档位：`xhigh`
- 运行方式：单一 Builder、标准模式；未启用子 Agent 或多 Agent
- 可观察起始时间：`2026-08-18T16:48:35+0800`
- 结束/冻结决定时间：`2026-08-18T16:56:06+0800`
- 时间复现限制：应用未暴露精确任务派发时间；起始时间为本任务首次捕获的本地 ISO 时间，不反推更早的秒级时间。
- 模型复现限制：应用未暴露精确底层模型快照或构建号；不作猜测。

## 输入白名单与校验

运行开始时，工作区顶层研究输入只包含以下四个文件；未发现其他研究输入、Gold、讨论日志或既有 Builder 输出。

| 文件 | 字节数 | SHA-256 | 状态 |
|---|---:|---|---|
| `paper.pdf` | 1496206 | `268ad0f67004844b252e80fee7f6f1724fc9a52506956f7e15b5cd2601371752` | 与 `INPUT_MANIFEST.sha256` 一致 |
| `paper.txt` | 62725 | `34f1b86c82eb5eef5547bf6a7c09317b28ad51e3ed7cf985406104456f68ca63` | 与 `INPUT_MANIFEST.sha256` 一致 |
| `BUILDER_PROTOCOL_V1.md` | 11423 | `3fed6a5ce3d4f7df0d99c6a49576ce9a4932d2097e0d51c93bbe9f32c33aedac` | 与 `INPUT_MANIFEST.sha256` 一致 |
| `INPUT_MANIFEST.sha256` | 241 | 不自校验 | 仅列前三项哈希 |

- 权威来源：`paper.pdf`
- `paper.txt` 用途：本次没有用其支撑任何正式判断；所有证据索引均回到 PDF。
- 网络/外部资料：未使用。
- Gold/评分规则/讨论日志：未访问、未索取、未推测。
- 四个输入文件：未修改；冻结前再次执行前三项哈希校验并全部返回 `OK`。

## 预注册流程执行

| 项目 | 预注册上限/要求 | 实际执行 |
|---|---|---|
| 主运行候选 | 仅一次，不按质量重跑 | 1 个正式候选；无候选选择 |
| 第一轮 | 完整局部自下而上构建 | 完成：15 页全读、全页渲染、物理结构、U/A/E 骨架 |
| 第二轮 | 全局自上而下构建 | 完成：摘要—方法—结果—限制回查，建立 R 与五问式计划 |
| 第三轮 | 双向核验 | 完成：模板、表 3–6、限制、附录与失败案例复核 |
| 有界定向修正 | 最多 2 次，仅由停止门缺口触发 | 0 次；停止门无缺口 |
| 完整主运行技术重试 | 最多 1 次 | 0 次 |
| 子 Agent/多 Agent | 禁止 | 0 |

## 技术失败与恢复记录

- 技术事件数：1 个 PDF 工具可用性恢复事件。
- 记录时间：`2026-08-18T16:48:35+0800`。
- 可观察错误：系统路径缺少 `pdftotext`；通用 Python 环境缺少 `fitz`。
- 已产生文件：失败的 `pdftotext` 调用未产生文件；`fitz` 尝试仅创建空的 `tmp/pdfs` 目录，不产生页面文件。
- 处置：使用已提供的本地 `pdfplumber` 直接读取 PDF，并使用已提供的本地 `pdftoppm` 成功渲染 15 页；未联网、未安装依赖、未更换模型、未重启主运行。
- 是否构成完整主运行重试：否。

## 重要修订

1. source/reference 从“所有条件必然必要”降格为表 3 支持的强总体趋势（T3>T1 与 T3>T2 均为 43/48），保留例外。
2. CoT 从“改善大模型表现”降格为混合局部现象（T7>T3 为 10/48），不作普遍因果陈述。
3. 资源水平差异并列保留分数分布机制与模型/数据覆盖解释，未强行合并。

## 停止门

- 覆盖度：`PASS`。主体各节、核心论证、表 1–7、图 1–6、限制、伦理与附录均被覆盖或明确定位。
- 证据度：`PASS`。持久判断与关系均有 PDF 证据；作者推测、Builder 解释和显著缺失已区分。
- 稳定度：`PASS`。第三轮及冻结前机械校验未再改变主干判断或 GenerativePlan。
- 正式停止状态：`COMPLETE`
- 未完成项：无。
- 定向修正次数：0/2。

## 冻结输出包

1. `BUILDER_OUTPUT.md`
2. `BUILD_TRACE.md`
3. `RUN_MANIFEST.md`
4. `OUTPUTS.sha256`

`OUTPUTS.sha256` 生成后，前三个文件不得原位修改。本运行不生成多个候选或更正版本。
