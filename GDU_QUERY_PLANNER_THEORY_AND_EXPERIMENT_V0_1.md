# GDU 查询规划器：理论选择与首轮实验 v0.1

## 1. 当前问题

GDU 已经能够验证论证并按接受状态输出答案，但此前仍需人工指定目标命题。查询规划器负责把自然语言问题转换为：

- 问题意图；
- 涉及的概念；
- 时间与公司口径；
- 目标命题和限制命题；
- 找不到目标时的显式缺口。

它只负责规划，不负责绕过论证系统直接回答。

## 2. 可借鉴理论及取舍

### 2.1 语义解析与查询图生成

知识图谱问答通常先把问题映射为查询图，再执行查询。Li 与 Ji 的研究进一步表明，先判断问题的语义结构可以减少噪声候选查询图。来源：[Semantic Structure Based Query Graph Prediction](https://aclanthology.org/2022.coling-1.135/)。

本轮采纳“先识别结构，再选目标”的原则。目前只实现三种最小结构：

- 对比解释；
- 命题核验；
- 缺失因果因素。

没有直接采用 BERT 排序器，因为当前样本规模不足，训练模型只会增加复杂度而不能提供可信收益。

### 2.2 不完整知识图上的检索与推理

PullNet 面向知识库不完整的场景，逐步构造与问题相关的局部子图，并在必要时从知识库和文本共同取回信息。来源：[PullNet](https://aclanthology.org/D19-1242/)。

GDU 采纳“问题相关子图”和“图缺失时回查文本”两个原则，但不采用其 GCN 作为最终推理器。原因是 PullNet 的答案选择主要是学习得到的，而 GDU 要求最终路径通过显式逻辑接受检查。

### 2.3 受约束扩散激活

受约束扩散激活通过语义网络寻找用户没有直接说出但可能相关的信息；关键是限制传播，而不是沿所有边无限扩散。来源：[Information retrieval by constrained spreading activation in semantic networks](https://doi.org/10.1016/0306-4573(87)90017-3)。

本轮实现最多两跳的有类型邻域扩散，区分推理正向、推理反向和冲突边。已能回答的问题不执行扩散，只有发现缺口时才启动。

### 2.4 图路径查询

SPARQL 1.1 将属性路径定义为图节点之间的有向路线，并支持序列、选择、逆向和任意长度路径。来源：[W3C SPARQL 1.1 Property Paths](https://www.w3.org/TR/sparql11-query/#propertypaths)。

本轮只采纳“路径有方向、边有类型、长度受控制”的抽象，不引入 RDF 存储和 SPARQL 引擎。当前 Python 图结构足以验证接口，引入完整技术栈还没有实验必要。

### 2.5 暂缓采用 Personalized PageRank

HippoRAG 使用知识图和 Personalized PageRank 进行关联检索，并报告了多跳问答上的收益。来源：[HippoRAG](https://arxiv.org/abs/2405.14831)。

该方法适合图规模扩大后的候选排序，但当前真实切片只有少量节点。现在加入 PageRank，无法判断收益来自理论还是参数。因此将其保留为后续与“两跳受约束扩散”的对照项，而不是当前默认模块。

## 3. 理论能否组合

目前组合关系是顺序接口，而不是把所有理论揉成一个算法：

1. 语义解析产生问题结构和 Context；
2. 查询结构确定目标 Atom；
3. Dung 接受状态筛除不可用论证；
4. 找不到被接受的目标时，受约束扩散寻找邻近命题；
5. 图仍缺失时，才按 PullNet 思路定向回查原文；
6. 新证据与新命题进入隔离区，通过逻辑和来源验证后才能成长进正式图。

这些理论当前没有职责重叠：语义解析负责“找什么”，扩散负责“从哪里继续找”，论证语义负责“找到的能不能回答”，TMS/PROV 负责“以后变化时怎么撤销和追溯”。

## 4. 首轮实验结果

### 4.1 同义问法

三种表达均映射到同一目标 `C-PROFIT-OCF-BRIDGE`：

- 为什么净利润亏损但经营现金流为正；
- 利润是负的，经营活动现金流却为正，怎么解释；
- 为何亏损而经营现金流仍然为正。

状态均为 `ready`，且没有启动邻域扩散。

### 4.2 带错误前提的问题

“经营现金流为正，是不是说明全年现金增加”被解析为命题核验。规划器选择原文的现金净减少命题和指标边界命题，没有选择已被拒绝的“现金增加”经验推断。

### 4.3 Context 错配

“母公司为什么净利润亏损但经营现金流为正”不能复用合并口径命题，状态为 `gap`，原因为 `no_accepted_claim_in_context`。

### 4.4 缺节点与原文回查

“哪项投资活动导致全年现金减少”需要 Atom `investment_cash_change_driver`，正式图中不存在，因此系统没有猜答案，而是：

1. 从现金变化和投资相关邻近命题进行最多两跳扩散；
2. 生成受限原文检索词；
3. 命中第 15、19、72、73 个物理页；
4. 核对第 72—73 页跨页合并现金流量表；
5. 生成隔离候选，而不直接修改正式图。

候选数值核对：

- 投资活动流入：69,525,306.42 元；
- 投资活动流出：306,155,126.21 元；
- 投资活动净流出：-236,629,819.79 元；
- 经营、投资、筹资现金流与汇率影响合计：-187,884,922.38 元；
- 与报告的现金净变化残差：0.00 元。

候选结论保留边界：投资活动净流出是全年现金减少的重要组成部分，但不能单独解释全部现金变化。

## 5. 当前结论

查询规划器 V0 已证明四个接口能够共同工作：同义问法映射、错误前提纠正、Context 阻断、缺口触发的图到原文回查。

但新发现的投资节点仍处于 `candidate_pending_logic_validation`，不能进入正式答案。下一步只需把该候选转换成正式 AIF/ASPIC+ 结构并运行接受、冲突和 TMS 测试；通过后再形成第一次完整的“提问触发数据库生长”。
