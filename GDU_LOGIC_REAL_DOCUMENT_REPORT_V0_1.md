# GDU 真实文档逻辑映射实验报告 v0.1

## 1. 本轮在验证什么

本轮首次把候选逻辑架构用于真实年报，而不是继续使用抽象样例。材料为拉芳家化 2025 年年度报告第 156—157 个物理页的现金流量表补充资料。

问题是：为什么净利润为负，而经营活动现金流量净额为正；能否由此进一步判断全年现金增加？

实验对象同时包含：

- 原文证据及页码；
- 表内数字和符号；
- 从净利润到经营现金流的严格算术推导；
- 一个常见但不可靠的推断：“经营现金流为正，所以全年现金增加”；
- 对该错误推断的反驳和规则阻断；
- 某项证据失效后的局部重算。

## 2. 完整数值核对

| 组成部分 | 金额（元） |
|---|---:|
| 净利润 | -30,937,240.23 |
| 非现金等调节合计 | 38,398,698.24 |
| 营运资本调节合计 | 50,049,521.49 |
| 估值、投资、税项及其他调节合计 | 15,034,801.66 |
| GDU 计算的经营活动现金流量净额 | 72,545,781.16 |
| 原文报告金额 | 72,545,781.16 |
| 残差 | **0.00** |

旧增长实验只抽取了部分调节项，因此留下 15,707,544.30 元残差。本轮纳入全部表内调节项后残差归零。这也给出了一个新的建图约束：

> 摘要节点可以用于检索和解释，但只有通过完整项目核对的节点才能标记为“完整数值证明”。

## 3. 逻辑架构在真实材料中的工作方式

### 3.1 表示层

证据节点保存物理页码、表格位置和 PDF 哈希；命题节点分别保存净利润、三组调节项、经营现金流和现金净变动。金额使用十进制定点字符串，避免浮点误差。

### 3.2 论证构造层

系统形成以下可公开审计的推理路径：

1. 分别加总三组调节项目；
2. 将三组合计与净利润相加；
3. 将计算结果与报告金额比较；
4. 在完全相等后，形成“负利润与正经营现金流之间的差异已被解释”的桥接命题。

这里记录的是外显论证路径，不是模型不可检查的内部思维过程。

### 3.3 接受状态层

严格算术形成的桥接论证被接受。与此同时，系统还构造了一个可废止的经验推断：“经营现金流为正，所以全年现金增加”。它受到两种限制：

- 报表结构说明经营现金流与全部现金净变动不是同一指标，因此该推断规则被 `undercut`；
- 原文直接报告现金及现金等价物净减少 187,884,922.38 元，因此其结论被 `rebut`。

最终，严格对账解释被接受，错误的现金增加结论被拒绝。

### 3.4 四值信息状态

在未经论证筛选的原始信息层，系统同时拥有“现金增加”和“现金未增加”两种极性，Belnap 状态为 `BOTH`。这不是最终回答自相矛盾，而是明确记录“系统曾收到冲突信息”。Dung 接受语义随后给出：直接报表证据被接受，经验推断被拒绝。

两种状态不能合并为一个字段：一个负责保存信息冲突，一个负责决定当前可用论证。

### 3.5 依赖维护与修复

模拟“营运资本调节行需要复核”后：

- 原文报告的经营现金流事实仍有效；
- 营运资本合计失效；
- 计算出的经营现金流失效；
- 数值匹配和桥接解释随之失效；
- 与该证据无关的“经营现金流不能代表全部现金变化”路径不受影响。

局部重算与全图重算结果完全一致。这说明当前架构可以区分“文档说了什么”和“我们是否仍能证明为什么”。

## 4. 自动测试结果

真实文档新增 5 类测试：

- 接口合法性；
- 全项目精确对账；
- 正确论证接受、错误经验推断拒绝；
- 桥接结论到原始来源命题的完整追溯；
- 证据失效后的局部重算与全量重算一致。

与已有接口测试合并运行，共 `24 passed`；随后执行全仓回归，共 `179 passed, 5 skipped`。跳过项为原有条件性测试，本轮没有新增失败。

## 5. 阶段结论

这轮结果支持“三层逻辑主干＋两个横向模块”可以组合在真实材料中工作，且各部分承担不同职责，没有在接口处互相冲突。

同时只能得出有限结论：目前证明了一个真实财务推理切片能够被完整表达、核对、阻错和局部修复；还没有证明它能自动、稳定地从不同类型长文档中建出同等质量的图，也没有证明整体问答优于所有 Chunk RAG。

下一轮应继续做同一份真实图的问答执行器：让问题先选择论证路径，再输出结论、限制条件和逐条证据，而不是直接从节点拼接答案。
