# GDU 表示消融实验计划 v1

> 状态：运行前预注册  
> 日期：2026-08-20

## 1. 研究问题

完整 GDU 中哪些部分真正提高长文档问答质量，哪些部分可以删除或改为按需生成？

本实验不修改冻结 Schema、Protocol、Gold 或原始 Builder 输出。所有简化版本均由同一份冻结年报和冻结 GDU 确定性派生，避免为某道题人工补充答案。

## 2. 固定条件

- 使用 `GDU_VS_CHUNK_RAG_BENCHMARK_PLAN_V1.md` 已冻结的六个问题和 0–2 分评分项；
- 回答模型保持 `deepseek-v4-flash-0731`、温度 0、关闭思考；
- 每题检索上下文上限约 7,200 字符；
- 每个新增条件只运行一次批量调用；
- 不向回答模型提供 Gold、得分条件或其他条件的答案；
- 已完成的 Raw Chunk 与 Full GDU 结果直接复用，不重复付费调用。

## 3. 五个表示条件

### V0：Raw Chunk

原始 `paper.txt` 按物理页切块。只带物理页标签。复用已有 A 组结果。

### V1：Chunk + Structure

仍使用原文页块，只增加由标题正则确定性继承的当前顶层章节标签。不增加事实摘要或人工关系。

目的：检查章节元数据本身是否已能改善检索。

### V2：Assertion Nodes

只保留冻结 GDU 的“原子判断”和“证据索引”，删除：

- 文档来源说明；
- 物理结构说明；
- 语义单元；
- 所有显式关系；
- GenerativePlan；
- 未覆盖与不确定项汇总。

目的：检查结构化判断节点本身是否已经承担主要收益。

### V3：Minimal Graph

在 V2 基础上增加关系边，但把现有关系类型压缩为四类：

- `supports`：支持、核验、实例化或提供事实背景；
- `limits`：限制、口径限定、鉴证边界、风险与信息缺口；
- `conflicts`：来源内冲突或不可静默合并的张力；
- `composes`：组成、操作化或扩展某个解释。

每条边仍保留端点、原关系说明和证据引用。目的：检查关系图相对“只有节点”的净增益。

### V4：Full GDU

使用完整冻结 `BUILDER_OUTPUT.md`。复用已有 B 组结果。

## 4. 预注册判定

每个条件总分 12 分，同时单独报告 Q3–Q6 的关系/限制/冲突分数。

复杂度按持久化表示中的字符数、检索块数和对象类别数记录，不把构建时模型费用隐藏在答案分数中。

删除规则：

1. 若更简表示总分不低于更复杂表示，且没有新增高影响错误，则删除新增结构；
2. 若总分下降 1 分，但持久化字符数或对象类别明显下降，记为可选功能，不进入最小核心；
3. 只有当增加关系使总分至少提高 2 分，或单独修复一项高影响边界/冲突时，关系层才被判为最小核心必需；
4. Full GDU 若不高于 Minimal Graph，GenerativePlan、语义单元和完整物理结构不进入查询时最小表示，可保留为构建过程或按需视图；
5. 单篇文档结果只用于选择下一种原型，必须在新文档复验后才能冻结通用架构。

## 5. 预期输出

- 五条件分数与逐题差异；
- 表示大小、块数和调用次数；
- 最小有效 GDU 的候选数据结构；
- 明确保留、按需生成和删除的组件清单。
