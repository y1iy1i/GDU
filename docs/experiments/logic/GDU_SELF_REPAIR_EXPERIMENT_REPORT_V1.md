# GDU 推理路径自修复实验报告 v1

> 日期：2026-08-20  
> 文档：拉芳家化 2025 年年度报告  
> 结论：确定性证据错误能够在回答前被阻断并以新版本修复；语义型自修复尚未验证。

## 1. 实验目的

验证 GDU 在沿知识图回答问题时，能否发现节点与原文证据不一致、阻止错误继续传播，并在不覆盖历史记录的前提下形成修复版本。

本实验不是 GDU 与 Chunk 的最终优劣比较，也不测试自动 Builder 的召回率。它只验证问答阶段的安全门和修复闭环。

## 2. 实验材料

从冻结的拉芳家化年报 Gold 中建立一个人工校验的现金流推理切片：

- 4 个证据锚点；
- 5 个判断/限制节点；
- 4 条 `supports` 或 `limits` 关系；
- 涵盖净利润、经营现金流、存货方向和现金净变化。

Gold GDU 切片 SHA-256：

`9356f992914d46b792e44b6b14529ee646e918a64e1835c9c087e676af3fcbd7`

## 3. 四种情形

| 情形 | 预期行为 | 实际结果 |
|---|---|---|
| 正确路径 | 允许回答 | `answer_allowed` |
| 注入“存货减少”错误 | 阻断回答并要求修复 | `blocked_repair_required` |
| 批准证据型修复 | 保留旧节点，建立 v2 并重新允许回答 | 通过 |
| 路径含两个显式冲突解释 | 不强行合并，带不确定性回答 | `answer_with_uncertainty` |

注入错误复现了上一轮 Builder 的真实问题。原文为：

> 存货的减少（增加以“－”号填列） -2,182,144.36

节点错误地把负数解释为“存货减少”。审计器根据表头规则得到 `increase`，在答案生成前阻断该节点，并产生只绑定该证据的修复提案。

## 4. 修复后的版本行为

- 原节点 `A-CF-03` 没有删除，状态变为 `superseded`；
- 新节点为 `A-CF-03-v2`，状态为 `machine_verified`；
- 关系以新节点克隆为新版本，不覆盖旧边；
- 修复日志标记所有受影响路径必须重算；
- 未批准提案时，程序拒绝写入。

## 5. 验证结果

- 自修复实验：`SELF_REPAIR_STAGE_PASS`；
- 新原型与上一轮外部复验的针对性测试：8 项全部通过；
- 远程模型调用：0；
- 没有产生 API 费用。

## 6. 当前能够与不能够说明的内容

已经证明：

1. 图中的错误不必等到最终答案产生后才发现；
2. 表格符号等可确定解析的问题，可以通过证据规则自动拦截；
3. 修复可以采用版本化补丁，保持完整历史；
4. 显式矛盾可以触发保留分歧，而不是让模型擅自选择。

尚未证明：

1. 模型能可靠发现所有语义矛盾；
2. 自动 Builder 能构建完整正确的图；
3. Gold GDU 在多跳查询中优于 Chunk；
4. 所有自动修复都可以免人工审核。

## 7. 下一实验

下一步使用 Gold GDU 与 Raw Chunk 进行关系敏感实验。问题必须依赖多个节点、范围限制或版本变化才能回答，并比较：

- 答案正确性；
- 推理路径完整性；
- 原文证据追溯率；
- 错误节点的传播范围；
- 修改一个事实后的重算成本；
- 严格上下文预算下的表现。

只有这一步才能开始回答“正确构建的 GDU 到底是否比 Chunk 更好用”。

