# DeepSeek V4 Flash 0731 远程 CP6 停止门实验报告

日期：2026-08-20

状态：停止门判断通过；完整文档冻结被正确拒绝，当前产物只能保持 provisional。

## 实验简介

CP6 不再生成新知识，而是判断现有工作是否足以停止构建并冻结 GDU。本轮只完成了年报第二节的局部纵向实验，因此正确答案不是“全部通过”，而是诚实指出完整文档仍缺什么。

## 实验设置

- 服务：阿里云百炼北京区 Token Plan；
- 模型：`deepseek-v4-flash-0731`；
- 输入：CP5 局部生成计划、已观察页范围和能力范围清单；
- 阶段：CP6，只允许 stop_gate；
- 实际 API 请求：1 次；
- 自动重试：0；
- 定向修正：0。

## 预登记预期

- `coverage=failed`：237 页文档只有封面与第二节局部样本被建模；
- `evidence=passed`：当前局部对象具有文本层证据；
- `stability=passed`：最终局部快照在已完成修正后可保持；
- `cross_carrier=failed`：尚未核验表格视觉结构、图像和公式等载体；
- `cross_section=failed`：尚无其他章节语义单元，不能检查跨章节关系；
- `negative_boundary=passed`：局部计划已明确不能排序因素、不能推广到全文。

## 结果

模型首次请求即返回与预登记完全一致的六项状态，并生成三个具体 Gap：

1. 全文覆盖 Gap：物理页 2–7、13–237 尚未建立结构与语义覆盖，最早返回 CP1；
2. 跨载体 Gap：物理页 9–12 只验证文本层，最早返回 CP1；
3. 跨章节 Gap：其他主要章节尚未形成语义单元，最早返回 CP2。

最终发布决定为：`provisional_only_do_not_freeze`。

- Adapter response Schema：通过；
- CP6 停止门语义验收：通过；
- 原始结果 SHA-256：`c07067f873cf0870b7ac6c710da4bc8c65e079b79e2c95ccbe146b46eeecdf7d`。

## 结果解读

CP6 的成功体现为系统能够拒绝过早成功。CP1–CP5 在局部样本上通过，不代表整篇文档已经被理解。停止门把“局部能力可行”和“完整 GDU 可冻结”明确分开，避免用一次小实验包装成完整成果。

## 当前阶段结论

本项目已经完成从 CP1 到 CP6 的首条真实模型局部纵向链路：结构、局部语义、并行解释与不确定性、关系、局部计划、停止判断均得到实际结果。

但这条链路的最终状态是 provisional，而不是 frozen。下一轮工作应根据三个 Gap 扩大文档覆盖和载体能力，而不是修改 Schema 或宣布完整 GDU 已完成。
