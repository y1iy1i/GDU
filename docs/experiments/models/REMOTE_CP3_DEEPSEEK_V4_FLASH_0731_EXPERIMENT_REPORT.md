# DeepSeek V4 Flash 0731 远程 CP3 实验报告

日期：2026-08-20

状态：一次定向修正后通过；尚未进入 CP4。

## 实验简介

CP3 测试 GDU 能否把事实、分析性解释和不确定性分开保存。实验问题是：2025 年利润改善应如何解释？

原文同时列出算力业务盈利增加、制造端亏损收窄和对外投资公允价值正向变动，但没有完整量化三者各自的贡献。系统因此不能为了得到一个简洁答案而擅自选出“最主要原因”。

## 实验设置

- 服务：阿里云百炼北京区 Token Plan；
- 模型：`deepseek-v4-flash-0731`；
- 输入：已接受的 CP2 结果，以及年报物理页 9–12；
- 阶段：CP3，只允许 assertion 与 interpretation_group；
- 每次运行最大调用次数：1；
- 实际 API 请求：2 次；
- 自动重试：0；
- 定向修正：1 次。

## 预登记验收条件

1. 生成一条来源归因断言，完整保留原文列出的三类利润增长原因；
2. 分别生成“经营改善”和“投资公允价值变化”两个分析性解释；
3. 使用 `parallel` 解释组同时保留两个方面，不设置 `preferred_ref`；
4. 保存一条 `undetermined` 约束，明确现有披露不能支持贡献排序；
5. 任何正向解释都不能声称某因素最大、主导、首要、唯一或完全因果；
6. 所有新对象使用临时 handle，由 Builder 分配规范 ID。

## 运行过程

### 初始生成

- 模型在候选 `fields` 中自行写入正式 `id`；
- 违反 Builder 独占规范 ID 分配权的边界；
- 候选在 ID 分配阶段被拒绝，未进入 Builder；
- 当时脚本尚未在 ID 分配前保存候选，因此没有可核验的原始结果文件；调用次数与错误类型已记录。

### 第一次定向修正

- 模型不再自行分配 ID；
- 返回 4 条 assertion 和 1 个 interpretation_group；
- Adapter response Schema、候选 ID 分配、GDU 字段 Schema 均通过；
- 原始结果 SHA-256：`8957412dcc9ac4e7e151eb3f65bdca0c9b4e824b5ddacfc6f9e320804dacd148`。

第一次本地语义判定错误地拒绝了这份结果。原因是验收器在所有分析性断言中搜索“最大”，把约束句“不能判断哪一项贡献最大”误判为模型正在进行贡献排序。

修正后，禁止排序检查只作用于解释组中的两个正向解释，不作用于专门否定排序的约束断言。新增回归测试后，同一份原始模型结果离线重验通过，没有再次调用 API。

## 最终对象

- `A-005`：来源归因断言，保留原文列出的三类增长原因；
- `A-006`：经营改善解释，涵盖算力盈利增加和制造端亏损收窄；
- `A-007`：投资公允价值解释，结合富乐德股票公允价值变动证据；
- `A-008`：`undetermined` 约束，禁止在缺少完整贡献分解时排序；
- `IG-001`：`parallel` 解释组，同时保留 `A-006` 与 `A-007`，不设置首选。

## 结果解读

本轮证明模型能够在给定事实与结构内，同时保存两个有证据支持、互不排斥的解释，并明确记录当前不能回答的问题。这是 GDU 与普通单答案摘要的重要设计差异：系统可以给出当前理解，但不必通过删除合理可能性来制造确定感。

实验也暴露出两类不同风险：模型可能越权分配 ID；本地验收器也可能误解正确的不确定性表达。研究结论必须同时审查生成端和评估端，不能默认“验收失败一定是模型错误”。

## 当前结论边界

可以得出：在限定页包和一次定向修正内，DeepSeek 能生成合格的并行解释组与显式不确定性边界。

不能得出：模型已能自动发现整篇文档的所有重要争议、稳定处理真正冲突的竞争解释、完成跨章节关系，或证明 GDU 优于现有知识存储体系。
