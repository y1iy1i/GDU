# v0 档案说明

本目录保存已冻结或已被后续研究主线取代的早期资产。

这些文件没有被删除，因为它们仍用于：

- 复现实验；
- 核对设计决策；
- 验证历史哈希；
- 判断当前架构相对早期方案发生了什么变化。

除非明确开展历史基线修复，否则不要修改带 `BASELINE` 的文件及其对应 `.sha256`。文件移动只改变仓库位置，没有改变文件内容。

当前系统入口位于 `../../current/`，完整导航位于 `../../README.md`。
