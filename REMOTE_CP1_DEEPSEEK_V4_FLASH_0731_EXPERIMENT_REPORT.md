# DeepSeek V4 Flash 0731 远程 CP1 边界实验报告

日期：2026-08-20

状态：通过机械校验与本次章节边界语义验收；尚未进入 CP2。

## 大白话

这次给模型看了年报第 1 页和第 8–12 页。DeepSeek 不只是交回了格式正确的 JSON，还真的使用了第 8、9、10、11、12 页：它把“第二节 公司简介和主要财务指标”识别为第 8–12 页，并在第 12 页识别出“第三节 管理层讨论与分析”开始。

因此，DeepSeek 在这个具体的 CP1 小实验中通过了。这个结果不能证明它已经能自动理解整份 237 页年报，也不能证明完整 GDU 优于其他知识存储体系。

## 实验设置

- 服务：阿里云百炼北京区 Token Plan；
- 模型：`deepseek-v4-flash-0731`；
- 输入：Pilot 03 年报物理页 1、8–12；
- 阶段：CP1，只允许 evidence 与 physical_structure；
- JSON 模式：原生；
- 思考模式：关闭；
- 最大调用次数：1；
- 实际调用次数：1；
- 自动重试：0。

## 机械结果

- 返回 9 个规范化对象：3 个 physical_structure、6 个 evidence；
- Adapter response v1 Schema：通过；
- GDU manifest 与字段 Schema：通过；
- 授权 PDF 片段逐字接地：通过；
- 本地结果：`tmp/remote_cp1_deepseek_v4_flash_0731/boundary_8_12.json`（Git 忽略）；
- 原始结果 SHA-256：`e33f72340442caf253b1c1e4688a94148d54e92fcf4bb113210ca1410c991559`。

## 语义验收

- 文档节点：1–237，正确；
- 第二节标题：“第二节 公司简介和主要财务指标”，正确；
- 第二节范围：8–12，与冻结人工参考一致；
- 第二节证据：覆盖第 8–12 页，而不是只引用起始页；
- 后继边界：在第 12 页识别出“第三节 管理层讨论与分析”开始，支持第二节终点判断。

## 与 Qwen 的同条件对照

Qwen 在相同输入上通过机械校验，但只引用第 1、8 页，并把第二节保留为 8–8。DeepSeek 同时通过机械门和边界语义门。因此后续正式实验默认使用 DeepSeek，Qwen 保留作失败对照。

## 当前结论边界

可以得出：`deepseek-v4-flash-0731` 在这个预先限定的 CP1 页包上，能生成机械合格、逐字接地且章节边界正确的候选。

不能得出：模型已能独立选页、完成 CP2–CP6、处理表格视觉结构、构建完整 GDU，或使 GDU 在问答实验中优于 Chunk/RAG、PageIndex、知识图谱。
